[build]
  publish = "."

# --- Certified Hooper -----------------------------------------------------
# Bare domain (certifiedhooper.netlify.app/) opens the app, not the old
# landing. Exact-root match only — every other page is served normally.
[[redirects]]
  from = "/"
  to = "/hoopsheaven.html"
  status = 302
  force = true

# --- Athlete OS -----------------------------------------------------------
# Clean short entry: /os → the Athlete OS landing (real folder, no loop).
# One-hop 302 to the real /athlete-os/ directory (which serves index.html
# and resolves relative assets correctly). No self-referential redirect.
[[redirects]]
  from = "/os"
  to = "/athlete-os/"
  status = 302

# --- Hoops Heaven ---------------------------------------------------------
# The product was briefly live as hoopshaven.html — keep that link alive.
[[redirects]]
  from = "/hoopshaven.html"
  to = "/hoopsheaven.html"
  status = 301

# --- NOVA Rewards (giveaway) ---------------------------------------------
# publish stays "." so the existing site root and other products are
# untouched; the config below is scoped to /giveaway/ only.

# Clean path: /giveaway (no trailing slash) → canonical folder.
[[redirects]]
  from = "/giveaway"
  to = "/giveaway/"
  status = 301

# Security headers for the giveaway app.
[[headers]]
  for = "/giveaway/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# --- Coachbuilt -----------------------------------------------------------
# Short links: /coachbuilt is what goes in the IG bio and every DM, /kit is
# the private playbook. Both are plain pages, so no publish-dir change.
[[redirects]]
  from = "/coachbuilt"
  to = "/coachbuilt.html"
  status = 301

[[redirects]]
  from = "/kit"
  to = "/coachbuilt-kit.html"
  status = 301
